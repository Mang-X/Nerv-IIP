{
  "$schema-note": "Registry consumed by scripts/verify-apphost-base-url-injection.ps1. Each entry exempts exactly one (consumerResource, key) pair of exactly one violation kind, must name a tracking issue, and must match a violation that really exists right now — a registration that stops matching turns the gate red instead of rotting in place. See scripts/lib/AppHostBaseUrlInjection.ps1 for what this list is and is not allowed to do.",
  "exemptions": []
}
