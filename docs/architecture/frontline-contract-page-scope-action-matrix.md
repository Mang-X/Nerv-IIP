# 现场作业契约—页面—范围—动作事实矩阵

本文是 MAN-620 / GitHub
[#1157](https://github.com/Mang-X/Nerv-IIP/issues/1157) 的仓库事实盘点，覆盖
MES、WMS、Quality、Maintenance、设备报警、Business PDA、Business Console
以及规划中的工位机、车间机表面。它回答“当前公开契约和页面实际能做什么”，不把类型名、
页面按钮、HTTP 200 或组织级列表推断为已经具备个人任务、完整生命周期或可执行动作。
WMS 行已于 2026-07-30 按 MAN-629 的主体范围、派工、任务动作与盘点权限边界复核。

## 1. 使用边界

### 1.1 证据优先级

1. BusinessGateway 公开 OpenAPI：
   [`business-gateway-console.v1.json`](../../frontend/packages/api-client/openapi/business-gateway-console.v1.json)。
2. Gateway 授权和代理实现：
   [`BusinessGatewayAuthorization.cs`](../../backend/gateway/BusinessGateway/src/Nerv.IIP.BusinessGateway.Web/Application/Auth/BusinessGatewayAuthorization.cs)、
   [`AuthorizedBusinessProxyEndpoint.cs`](../../backend/gateway/BusinessGateway/src/Nerv.IIP.BusinessGateway.Web/Application/Auth/AuthorizedBusinessProxyEndpoint.cs)
   以及各域 `BusinessConsole*Endpoints.cs`。
3. generated client 和稳定导出：
   [`types.gen.ts`](../../frontend/packages/api-client/src/generated/business-console/types.gen.ts)、
   [`business-console.ts`](../../frontend/packages/api-client/src/business-console.ts)。
4. 业务服务聚合、命令和查询实现。
5. 当前页面与 composable。页面按钮只证明“前端尝试调用”，不能单独证明领域允许。

所有下表 operation ID 均已同时出现在 OpenAPI 和 generated client；当前页面消费的
operation 也已从 `@nerv-iip/api-client` 稳定 barrel 导出。本文不手改以上任何契约或生成文件。

### 1.2 作业范围口径

MAN-619 / GitHub
[#1156](https://github.com/Mang-X/Nerv-IIP/issues/1156) 尚未合入仓库文档，因此本文只采用其
任务简报约束的五级定义，不复制未合入分支内容：

| 范围           | 本文判定标准                                                                                                              |
| -------------- | ------------------------------------------------------------------------------------------------------------------------- |
| `Self`         | 服务端以已认证当前 principal 验证 assignment/ownership 后过滤；前端传任意 user ID、页内筛选或行上恰好出现本人 ID 均不算。 |
| `Team`         | 服务端从当前主体的有效班组成员关系裁决，不信任客户端自行声明 team。                                                       |
| `WorkCenter`   | 服务端验证请求的工作中心属于当前主体授权范围。仅有 `workCenterId` 查询参数不等于已实现该裁决。                            |
| `Workshop`     | 服务端验证请求的车间属于当前主体授权范围。                                                                                |
| `Organization` | 只以 `organizationId + environmentId` 为硬 scope 的组织业务队列；它不是“我的任务”。                                       |

MAN-627 / [#1164](https://github.com/Mang-X/Nerv-IIP/issues/1164) 已交付
`GET /api/business-console/v1/me/work-context`：它从实时 IAM 授权取得当前 principal，
按本次 permission 的 role/membership grant 与 MasterData 候选范围求交，返回
candidate/authorized scopes、可用 scope kinds、角色来源和 freshness。该 API 只证明范围
选择已被服务端验证，不会自动替各域列表/命令下推范围；下表中的 `WorkCenter` 过滤若仍未
接入该上下文或没有资源归属校验，仍只记为“Organization 内业务过滤”。

### 1.3 状态标签

| 标签        | 含义                                                                           |
| ----------- | ------------------------------------------------------------------------------ |
| **当前**    | 当前公开契约和页面已有可验证事实。                                             |
| **P0 缺口** | 会造成错对象写入、伪个人任务、终态误操作、重复写或不可信成功回执，必须先收口。 |
| **P1 缺口** | assignment、范围、搜索、分页、完整生命周期或角色化表面后续能力。               |
| **等数据**  | 契约/页面存在，但目标环境缺遥测、历史或维修事实；不得造数或作为前端缺陷修复。  |
| **待裁决**  | 产品/交互/共享协议需由归属 issue 决定，本文不替实现 issue 定设计。             |

## 2. 横切契约事实

### 2.1 当前授权、错误和回执

- Gateway 每请求先校验 bearer、`organizationId + environmentId` 和 operation
  permission；401/403 是当前公开授权边界。前端按 permissionCodes 隐藏入口只属于 UX，
  不能替代 Gateway 裁决。
- 通用代理会保留下游非 2xx 状态码，因此真实下游 409/422 可以透传；但本矩阵涉及的
  OpenAPI operation 当前普遍只声明 200、400、401、403，MES 多数 operation 甚至未声明
  400。2xx 且 `success:false` 的下游响应会被统一转成 400。
- 当前没有跨域统一的 `OperationReceipt`。各命令返回各自的强 ID、状态或
  `accepted`；HTTP 200、本地 toast、乐观状态和 `accepted` 字段本身不能替代对应业务对象
  的权威回读。
- 下游 payload 无法解析时 Gateway 返回 502 `downstream-invalid-response`；前端不得把旧行、
  默认值或上次成功结果展示为本次成功。
- 统一 401/403/404/409/422/5xx、幂等键生命周期和权威回执由
  [#1162](https://github.com/Mang-X/Nerv-IIP/issues/1162) 负责；跨端回执组件和错误协议由
  [#1172](https://github.com/Mang-X/Nerv-IIP/issues/1172) 负责。

### 2.2 `allowedActions` 与阻塞原因现状

- MES 工序任务、可报工任务以及工单嵌套工序的 OpenAPI/generated DTO 已返回权威
  `allowedActions/blockReasons/evaluatedAtUtc`；Completed、Cancelled 和未知状态返回空动作。
- MES 工单详情原有 `blockingReasons`、MES dispatch 行原有 `blockingReasons` 仍是各自的
  阻塞事实，不等同于跨域通用动作许可。
- WMS 上架/拣货任务已返回服务端 `allowedActions/blockReasons`，并在每次动作后返回新的动作清单；终态为空。收货、出库、盘点仍以聚合状态、版本和领域错误裁决动作，不伪造同形字段。
- Quality、Maintenance、设备报警列表/详情仍没有统一的服务端动作清单或标准阻塞原因。
- 其余页面仍按本地 status switch、权限码或按钮可见性判断动作，状态缺失/未知时必须 fail
  closed。全端终态、未知态、冲突态统一门禁归
  [#1160](https://github.com/Mang-X/Nerv-IIP/issues/1160)，共享 `ActionGate` 协议归
  [#1172](https://github.com/Mang-X/Nerv-IIP/issues/1172)。

## 3. 契约—页面—范围事实矩阵

### 3.1 MES

MES Gateway 证据：
[`BusinessConsoleMesEndpoints.cs`](../../backend/gateway/BusinessGateway/src/Nerv.IIP.BusinessGateway.Web/Endpoints/Mes/BusinessConsoleMesEndpoints.cs)；
PDA 证据：
[`useBusinessMes.ts`](../../frontend/apps/business-pda/src/composables/useBusinessMes.ts)；
PC 证据：
[`useBusinessMes.ts`](../../frontend/apps/business-console/src/composables/useBusinessMes.ts)。

| 页面 / 业务对象                                                                       | 当前公开 operation 与强 ID                                                                                                                                                                                                             | 当前范围 / assignment                                                                                                                                                                 | 服务端筛选与分页                                                                                                                                       | 权限码                                                               | 动作、终态、未知态事实                                                                                                                                                                         | 结论 / 归属                                                                                                                                                                                                                                              |
| ------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------ | -------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| PDA 首页“我的任务”；PC `/mes/dispatch`；工序派工任务                                  | `listBusinessConsoleMesDispatchTasks`；`operationTaskId + workOrderId`，行另有 `assignedUserId`                                                                                                                                        | Organization 内 assignment/userId 业务过滤；PDA 把当前 `principalId` 当普通 `assignedUserId` 参数发送并在浏览器二次过滤，Gateway/服务端没有绑定已认证 principal，**不能证明 Self**    | `status,keyword,workCenterId,shiftId,deviceAssetId,assignedUserId,skip,take`                                                                           | `business.mes.dispatch.read`                                         | 行有 `blockingReasons`，无 `allowedActions`；持权调用方可查询任意 `assignedUserId`。Completed/Cancelled 不应进入首页，未知状态不应成为可执行主操作                                             | **当前** Organization dispatch。伪“我的任务”纠偏归 [#1163](https://github.com/Mang-X/Nerv-IIP/issues/1163)；principal-bound Self 查询归 [#1164](https://github.com/Mang-X/Nerv-IIP/issues/1164)、[#1165](https://github.com/Mang-X/Nerv-IIP/issues/1165) |
| PDA `/mes/operation`；PC `/mes/operation-tasks`；工序执行任务                         | `listBusinessConsoleMesOperationTasks`；`operationTaskId + workOrderId`                                                                                                                                                                | Gateway 以当前 principal、实时 permission-aware grants 与 MasterData work-context 校验 `scopeKind/scopeId`，再下推 Self/Team/WorkCenter/Workshop/Organization；客户端不能提交归属过滤 | `status,keyword,workCenterId,shiftId,deviceAssetId,workOrderId,scopeKind,scopeId,skip,take`；范围和业务筛选取 AND                                      | `business.mes.operations.read`                                       | MES 返回权威 `allowedActions/blockReasons/evaluatedAtUtc`；四类任务动作必须显式携带已选 `scopeKind/scopeId` 与 `idempotencyKey`；Completed/Cancelled 无动作 | **已交付契约与基础客户端门禁** [#1165](https://github.com/Mang-X/Nerv-IIP/issues/1165)：PC/PDA 消费对应写权限的服务端 `selectedScope`，缺失时不回读动作对象、不发动作命令；完整多范围选择与角色工作流仍分别归 [#1173](https://github.com/Mang-X/Nerv-IIP/issues/1173)、[#1174](https://github.com/Mang-X/Nerv-IIP/issues/1174) |
| PC `/mes/work-orders`、`/mes/work-orders/{workOrderId}`；PDA 报工/发料/入库的工单选择 | `listBusinessConsoleMesWorkOrders`、`getBusinessConsoleMesWorkOrderDetail`；`workOrderId`，显示可用 `workOrderNo`                                                                                                                      | 列表与详情都按当前主体经授权的 Self/Team/WorkCenter/Workshop/Organization 范围查询；详情先以相同范围 predicate 精确命中 `workOrderId`，再读取对象                                     | 列表：`status,keyword,workCenterId,shiftId,deviceAssetId,scopeKind,scopeId,skip,take`；详情：`scopeKind,scopeId`；total 与 page 复用同一范围 predicate | `business.mes.work-orders.read`                                      | 列表嵌套工序携带 MES 权威动作、阻塞和评估时间；详情在返回业务数据前实时重检 principal、permission 与对象归属，跨范围强 ID 返回 403                                                             | **列表与详情范围已交付** [#1165](https://github.com/Mang-X/Nerv-IIP/issues/1165)；PDA 准确绑定强 ID 归 [#1159](https://github.com/Mang-X/Nerv-IIP/issues/1159)                                                                                           |
| PC `/mes/work-orders`、`/mes/work-orders/{workOrderId}`；工单放行/挂起/取消           | `releaseBusinessConsoleMesWorkOrder`、`holdBusinessConsoleMesWorkOrder`、`cancelBusinessConsoleMesWorkOrder`；路径强 ID 均为 `workOrderId`                                                                                             | 每次动作按 `business.mes.work-orders.manage` 实时解析当前 principal 的所选范围，并以 MES 同一工单范围 predicate 精确命中强 ID；跨范围 fail closed                                     | 非列表；`scopeKind,scopeId` 只能选择本次 manage permission 已授权范围，每次写入仍以路径 `workOrderId + org/env` 定位                                   | `business.mes.work-orders.manage`                                    | release 需生产版本、工艺快照且无设备/质量/物料阻断；hold 拒绝终态；cancel 拒绝 Completed/Closed/Scrapped，已 Cancelled 为 no-op。公开回执仅 `accepted/downstream*`，须回读详情；未声明 409/422 | **对象范围已交付** [#1165](https://github.com/Mang-X/Nerv-IIP/issues/1165)；终态/冲突归 [#1160](https://github.com/Mang-X/Nerv-IIP/issues/1160)，幂等/权威回执归 [#1162](https://github.com/Mang-X/Nerv-IIP/issues/1162)                                 |
| PDA `/mes/report`；PC `/mes/production-reports`；生产报工                             | 新增 `listBusinessConsoleMesReportableOperationTasks` 作为报工选任务读面；写入仍由 `recordBusinessConsoleMesProductionReport(workOrderId + operationTaskId)`，回执 `productionReportId + reportNo`                                     | 可报工任务和写命令都按当前 principal 的 Self/Team/WorkCenter/Workshop/Organization 授权范围；写入前以相同任务范围 predicate 精确命中 `operationTaskId`，跨范围 fail closed            | 可报工任务支持 `status,keyword,workCenterId,shiftId,deviceAssetId,workOrderId,scopeKind,scopeId,skip,take`；写命令 body 必须携带所选 `scopeKind,scopeId` | 读 `business.mes.reporting.read`；写 `business.mes.reporting.write`  | 只有 InProgress 任务允许报工，Completed/Cancelled/Queued/Paused 均由服务端拒绝；任务行带 `allowedActions/blockReasons/evaluatedAtUtc`，写入继续使用 `idempotencyKey` 与强 ID 回执              | **选任务、对象范围、终态门禁与基础客户端门禁已交付** [#1165](https://github.com/Mang-X/Nerv-IIP/issues/1165)：PC/PDA 消费 reporting write 的服务端 `selectedScope`，缺失时禁用提交且不发命令；P0 对象一致性归 [#1159](https://github.com/Mang-X/Nerv-IIP/issues/1159)，统一回执归 [#1162](https://github.com/Mang-X/Nerv-IIP/issues/1162) |
| PDA `/mes/issue`；PC `/mes/materials`；线边发料/收料                                  | `listBusinessConsoleMesMaterialIssueRequests`、`createBusinessConsoleMesMaterialIssueRequest`、`confirmBusinessConsoleMesLineSideMaterialReceipt`；`requestId + workOrderId + operationTaskId?`                                        | Organization；无当前主体 assignment                                                                                                                                                   | 列表：`status,keyword,workCenterId,shiftId,deviceAssetId,workOrderId,skip,take`                                                                        | 读 `business.mes.materials.read`；写 `business.mes.materials.manage` | create/confirm 支持 `idempotencyKey`；公开回执是通用 `accepted/downstream*`，需回读 request 状态；终态/未知态没有标准动作清单                                                                  | **当前**命令存在；**P0 缺口**统一回执/终态 [#1162](https://github.com/Mang-X/Nerv-IIP/issues/1162)、[#1160](https://github.com/Mang-X/Nerv-IIP/issues/1160)                                                                                              |
| PDA `/mes/receipt`；PC `/mes/receipts`；完工入库请求                                  | `listBusinessConsoleMesFinishedGoodsReceiptRequests`、`createBusinessConsoleMesFinishedGoodsReceiptRequest`、`getBusinessConsoleMesFinishedGoodsReceiptInventoryLink`；`receiptRequestId/requestNo + workOrderId`，库存侧 `movementId` | Organization；无当前主体 assignment                                                                                                                                                   | 列表：`status,keyword,workCenterId,shiftId,deviceAssetId,workOrderId,skip,take`；库存关联按 `requestNo + workOrderId?`                                 | 读 `business.mes.receipts.read`；写 `business.mes.receipts.manage`   | create 支持 `idempotencyKey`，回执 `finishedGoodsReceiptRequestId + requestNo`；最终成功应回读 `receiptStatus/linkStatus/postedInventoryMovementId`，不能以请求创建成功代替库存过账            | **当前**两跳关联存在；**P0 缺口**统一权威回执 [#1162](https://github.com/Mang-X/Nerv-IIP/issues/1162)                                                                                                                                                    |

### 3.2 WMS

Gateway 证据：
[`BusinessConsoleWmsEndpoints.cs`](../../backend/gateway/BusinessGateway/src/Nerv.IIP.BusinessGateway.Web/Endpoints/Wms/BusinessConsoleWmsEndpoints.cs)；
PDA 证据：
[`useBusinessWms.ts`](../../frontend/apps/business-pda/src/composables/useBusinessWms.ts)。

作业范围三档的判定边界（[#1343](https://github.com/Mang-X/Nerv-IIP/issues/1343)，实现见
[`WarehouseWorkScopeAuthorizer.cs`](../../backend/services/Business/Wms/src/Nerv.IIP.Business.Wms.Web/Application/Auth/WarehouseWorkScopeAuthorizer.cs)）：

- `self` / `work-pool`：以 WMS 作业池成员资格为准（现场事实），无有效成员资格 fail closed；
  只返回已派工给本人 / 本作业池的记录。
- `site`：由 IAM **精确站点授权**直接成立，不再要求作业池成员资格——主管 / 管理员只有站点
  授权时也必须有可选范围，否则整域 403 全空。选中后是**整站作业面**（含站内尚未派工的记录），
  边界仍是精确站点授权，绝不跨站、绝不组织全量。
- 作业范围目录（`/wms/work-scopes/*`）返回空清单时，前端说「没有已授权的仓储作业范围」，
  403 透传服务端结论，不含糊成「请稍后重试」，也不伪装成「暂无数据」。

| 页面 / 业务对象                                  | 当前公开 operation 与强 ID                                                                                                                                            | 当前范围 / assignment                                                                             | 服务端筛选与分页                                                                                                | 权限码                                                                 | 动作、终态、未知态事实                                                                                                                                                     | 结论 / 归属                                                                                                                                                            |
| ------------------------------------------------ | --------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| PDA `/wms/inbound`；PC `/wms/inbound`；收货单    | `listBusinessConsoleWmsInboundOrders`、`assignBusinessConsoleWmsInboundOrder`、`completeBusinessConsoleWmsInboundOrder`；`inboundOrderId + inboundOrderNo`，完成回执 `requestId + inventoryMovementId` | Gateway 以当前 principal、permission-aware grants 与 MasterData 启用 site 目录校验 `self/work-pool/site`；聚合持久 `assignedOperatorUserId/assignedPoolCode/version` | `status,keyword,skuCode,uomCode,siteCode,locationCode,lotNo,serialNo,qualityStatus,ownerType,ownerId,scopeKind,scopeId,skip,take` | 读 `business.wms.receipts.read`；派工/完成 `business.wms.receipts.manage` | 受控派工需 `expectedVersion + idempotencyKey`；仅 Open 可完成，完成后可能进入 PendingQualityCheck 或 Completed，质量/库存失败仍由业务状态和回读回执表达 | **MAN-629 已交付主体范围与派工**；收货终态/回执按服务端状态和权威回读执行，不由 PDA 猜测 |
| PDA `/wms/putaway`；PC `/wms/putaway`；上架任务  | `list/create/assignBusinessConsoleWmsPutawayTask`；`start/recordProgress/reportException/completeBusinessConsoleWmsPutawayTask`；任务强 ID `warehouseTaskId`，来源 `inboundOrderId + lineNo` | 同上；只返回所选 `self/work-pool/site` 范围内已派工作业；工作池任务可在首次执行时由当前 operator 原子认领 | 列表 `status,keyword,locationCode,lotNo,scopeKind,scopeId,skip,take`；创建按来源订单/行定位 | 读 `business.wms.receipts.read`；创建/派工/执行 `business.wms.receipts.manage` | 行返回 `allowedActions/blockReasons/version`；动作携带所选范围、`expectedVersion + idempotencyKey`。进度为不可回退的累计数量，上架必须足量完成；终态动作清单为空 | **MAN-629 已交付派工与 start/progress/exception/complete**；PDA 不再是只读清单 |
| PDA `/wms/pick`；PC `/wms/picking`；拣货任务     | `list/create/assignBusinessConsoleWmsPickingTask`；`start/recordProgress/reportException/completeBusinessConsoleWmsPickingTask`；任务强 ID `warehouseTaskId`，来源 `outboundOrderId + lineNo` | 同上；持久 operator/pool/version，客户端不能注入 actor、授权 site 或内部归属过滤 | 列表 `status,keyword,locationCode,lotNo,scopeKind,scopeId,skip,take`；创建按来源订单/行定位 | 读 `business.wms.shipments.read`；创建/派工/执行 `business.wms.shipments.manage` | 行返回 `allowedActions/blockReasons/version`；动作携带所选范围、`expectedVersion + idempotencyKey`。累计进度不可回退/超计划，短拣完成必须提交差异原因；终态无动作 | **MAN-629 已交付派工与 start/progress/exception/complete**；拣货完成不再由出库父单冒充 |
| PDA `/wms/review`；PC `/wms/outbound`；出库/复核 | `listBusinessConsoleWmsOutboundOrders`、`assignBusinessConsoleWmsOutboundOrder`、`completeBusinessConsoleWmsOutboundOrder`；`outboundOrderId + outboundOrderNo` | 同上；聚合持久 operator/pool/version，列表按所选工作范围返回 | `status,keyword,locationCode,lotNo,scopeKind,scopeId,skip,take` | 读 `business.wms.shipments.read`；派工/完成 `business.wms.shipments.manage` | 派工使用版本和幂等键；仅 Open 且已有执行拣货数量、复核通过才可完成，随后回读 `inventoryPostingStatus/failureCode/failureMessage` 和 movement | **MAN-629 已交付主体范围与派工**；复核完成仍不等于拣货任务执行 |
| PDA `/wms/count`；PC `/wms/counts`；盘点执行     | `list/create/assign/completeBusinessConsoleWmsCountExecution`；`countExecutionId + countNo`，完成回执 `requestId + inventoryMovementId` | 同上；盘点聚合持久 operator/pool/version，列表与动作按 `self/work-pool/site` 校验 | `status,keyword,locationCode,scopeKind,scopeId,skip,take`；盘点当前无 lot 语义 | 读 `business.wms.counts.read`；创建/派工/完成 `business.inventory.counts.manage` | Open 可提交 `countedQuantity + expectedVersion + idempotencyKey`，Completed 不可变；同键同数量回放、异数量拒绝，差异与库存过账状态以精确回读为准 | **MAN-629 已交付独立读权限、主体范围与派工**；`receipts.read/manage` 均不授予盘点能力 |

### 3.3 Quality

Gateway 证据：
[`BusinessConsoleQualityEndpoints.cs`](../../backend/gateway/BusinessGateway/src/Nerv.IIP.BusinessGateway.Web/Endpoints/Quality/BusinessConsoleQualityEndpoints.cs)；
PDA 证据：
[`useBusinessQualityInspectionTasks.ts`](../../frontend/apps/business-pda/src/composables/useBusinessQualityInspectionTasks.ts)。

| 页面 / 业务对象                                                                                                | 当前公开 operation 与强 ID                                                                                                                                                                        | 当前范围 / assignment                                                                              | 服务端筛选与分页                                                                                  | 权限码                                                                                | 动作、终态、未知态事实                                                                                                                                                                                 | 结论 / 归属                                                                                                                                                                                                                               |
| -------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| PDA `/quality/tasks`；PC `/quality/inspection-tasks`；检验任务                                                 | `listBusinessConsoleQualityInspectionTasks`；`inspectionTaskId + inspectionPlanId + sourceDocumentId`                                                                                             | Organization；没有 inspector/team assignment。PDA 首页 emp034 的 pending total 不是个人待检        | 仅 `status,skuCode,skip,take`；sourceType、来源编号、超期、keyword 当前均不是服务端过滤           | `business.quality.inspection-records.read`                                            | 行无 `allowedActions/blockReasons`；Pending/InProgress/Completed 是领域状态，Completed 应只读。PC 对任意有 ID 的行显示“开始检验”                                                                       | **P0 缺口**伪“我的待检”纠偏 [#1163](https://github.com/Mang-X/Nerv-IIP/issues/1163)；**P1 缺口**派工/领取/搜索 [#1167](https://github.com/Mang-X/Nerv-IIP/issues/1167)                                                                    |
| PDA `/quality/tasks` 执行；检验提交                                                                            | `createBusinessConsoleQualityInspectionRecordFromTask`；路径 `inspectionTaskId`，回执 `inspectionRecordId + result + nonconformanceReportId/code + operationReceipt`                              | 写入 body 的 `inspectorUserId` 由 PDA 注入当前 principal，但服务端当前没有先验 assignment/领取裁决 | 非列表；特性另由 `listBusinessConsoleQualityInspectionPlanCharacteristics(inspectionPlanId)` 获取 | `business.quality.inspection-records.create`                                          | 必填意图级 `idempotencyKey` 绑定完整请求指纹与权威 `inspectionRecordId`；同键同载荷跨请求重放返回同一记录，同键异载荷拒绝，按 task 分布式锁收敛并发。数值和判定必须现场输入；当前仍无独立领取/转派命令 | **当前**持久幂等提交与权威记录 ID；**P1 缺口** assignment/领取冲突 [#1167](https://github.com/Mang-X/Nerv-IIP/issues/1167)，PDA 流程 [#1177](https://github.com/Mang-X/Nerv-IIP/issues/1177)                                              |
| PDA `/quality/record/{recordId}`、`/quality/ncr/{ncrId}`；PC `/quality/inspections`、`/quality/ncrs`；记录/NCR | `listBusinessConsoleQualityInspectionRecords`、`getBusinessConsoleQualityInspectionRecord`、`listBusinessConsoleQualityNcrs`、`getBusinessConsoleQualityNcr`；强 ID `inspectionRecordId`、`ncrId` | Organization；按 org/env 列表或强 ID 回读，无 assignment                                           | 两个列表均支持 `status,keyword,skip,take`；详情无列表过滤                                         | 记录读 `business.quality.inspection-records.read`；NCR 读 `business.quality.ncr.read` | 列表回执 `items + total`；详情终态应只读，缺 ID 或未知状态不得显示处置动作                                                                                                                             | **当前**列表/强 ID 下钻；统一列表壳归 [#1171](https://github.com/Mang-X/Nerv-IIP/issues/1171)，详情动作门禁归 [#1160](https://github.com/Mang-X/Nerv-IIP/issues/1160)，PDA 下钻归 [#1177](https://github.com/Mang-X/Nerv-IIP/issues/1177) |
| PC `/quality/ncrs`；NCR 处置/关闭/复检                                                                         | `submitBusinessConsoleQualityNcrDisposition`、`closeBusinessConsoleQualityNcr`、`createBusinessConsoleQualityReinspection`；`ncrId` 或前置 `inspectionRecordId`                                   | Organization；无个人 assignment                                                                    | 非列表                                                                                            | `business.quality.ncr.manage` 或 `business.quality.inspection-records.create`         | disposition/close 回执为通用 accepted；复检回执仅为新 `inspectionRecordId + attemptNumber`，result/NCR 必须再用 `getBusinessConsoleQualityInspectionRecord` 回读。合法状态由 Quality 聚合裁决          | **当前**命令存在；标准错误/终态归 [#1160](https://github.com/Mang-X/Nerv-IIP/issues/1160)、[#1162](https://github.com/Mang-X/Nerv-IIP/issues/1162)                                                                                        |

### 3.4 Maintenance

Gateway 证据：
[`BusinessConsoleMaintenanceEndpoints.cs`](../../backend/gateway/BusinessGateway/src/Nerv.IIP.BusinessGateway.Web/Endpoints/Maintenance/BusinessConsoleMaintenanceEndpoints.cs)；
PDA 证据：
[`useBusinessMaintenance.ts`](../../frontend/apps/business-pda/src/composables/useBusinessMaintenance.ts)。

| 页面 / 业务对象                                                           | 当前公开 operation 与强 ID                                                                                          | 当前范围 / assignment                                                                               | 服务端筛选与分页                                                        | 权限码                                                                       | 动作、终态、未知态事实                                                                                                                                                                   | 结论 / 归属                                                                                                                                                                               |
| ------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------- | ---------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| PDA `/equipment/repair`；PC `/maintenance/work-orders`；维修工单列表/详情 | `listBusinessConsoleMaintenanceWorkOrders`、`getBusinessConsoleMaintenanceWorkOrder`；`workOrderId + deviceAssetId` | Organization；行有 `assignedTechnicianUserId`，但列表没有 technician/current-user 过滤，不能称 Self | 仅 `deviceAssetIds,skip,take`；无 status、keyword、technician/team 过滤 | `business.maintenance.work-orders.read`                                      | 行无 `allowedActions/blockReasons`；领域只有 Open/Completed，Completed 不可变。PC 当前对所有行都展示“完成工单”，未先按终态撤销                                                           | **P0 缺口**终态门禁 [#1160](https://github.com/Mang-X/Nerv-IIP/issues/1160)；**P1 缺口**派工/筛选/生命周期 [#1168](https://github.com/Mang-X/Nerv-IIP/issues/1168)                        |
| PDA `/equipment/repair`；PC 工单新建；故障报修                            | `createBusinessConsoleMaintenanceWorkOrder`；回执 `workOrderId + operationReceipt`                                  | Organization；`openedBy` 来自当前登录名，`assignedTechnicianUserId` 是可选输入，不构成 Self 查询    | 非列表                                                                  | `business.maintenance.work-orders.manage`                                    | composable 为每次用户意图生成稳定 `idempotencyKey`，超时重试复用、确认成功后换键；服务端以同键载荷指纹返回同一工单，同键异载荷拒绝；可带 `sourceAlarmId`，但没有“报警到维修”原子复合命令 | **当前**防重与权威工单 ID；移动输入 [#1161](https://github.com/Mang-X/Nerv-IIP/issues/1161)，复合生命周期归 [#1168](https://github.com/Mang-X/Nerv-IIP/issues/1168)                       |
| PC `/maintenance/work-orders`；维修完成                                   | `completeBusinessConsoleMaintenanceWorkOrder`；路径 `workOrderId`，回执含 confirmed status/time/idempotencyKey      | Organization；可写 `actualTechnicianUserId`，但未验证为当前 Self/有权代操作                         | 非列表                                                                  | `business.maintenance.work-orders.manage`                                    | 仅 Open 可完成；意图键绑定完整完工载荷并按 workOrder 分布式锁串行，同键重放返回首次权威状态/时间，同键异载荷拒绝。接单、开工、暂停、待料、恢复、验证、关闭/取消仍不存在                  | **当前**一步完成已具持久幂等和权威回执；**P1 缺口**完整生命周期 [#1168](https://github.com/Mang-X/Nerv-IIP/issues/1168)，移动闭环 [#1178](https://github.com/Mang-X/Nerv-IIP/issues/1178) |
| PDA `/equipment/inspect`；PC `/maintenance/inspections`；设备点检         | `listBusinessConsoleMaintenanceInspections`、`recordBusinessConsoleMaintenanceInspection`；回执 `inspectionId`      | Organization；写入 inspector 为当前登录名，但列表无 inspector/current-user 过滤                     | 仅 `skip,take`；无 status、device、keyword 过滤                         | 读 `business.maintenance.plans.read`；写 `business.maintenance.plans.manage` | 请求无幂等键；结果/测量值现场输入。没有 assignment、领取、标准终态动作                                                                                                                   | **当前**点检记录；**P1 缺口**范围/目录 [#1168](https://github.com/Mang-X/Nerv-IIP/issues/1168)、[#1169](https://github.com/Mang-X/Nerv-IIP/issues/1169)                                   |
| PDA 点检计划选择；PC `/maintenance/plans`；保养计划                       | `listBusinessConsoleMaintenancePlans`；`planId + deviceAssetId`                                                     | Organization                                                                                        | `deviceAssetId,skip,take`；无 keyword/status                            | `business.maintenance.plans.read`                                            | 计划不是个人任务；PDA 当前按页加载并在客户端筛选，不得称全量搜索                                                                                                                         | **P1 缺口**服务器过滤和角色范围 [#1168](https://github.com/Mang-X/Nerv-IIP/issues/1168)                                                                                                   |

### 3.5 设备报警与设备数据

Gateway 证据：
[`BusinessConsoleEquipmentEndpoints.cs`](../../backend/gateway/BusinessGateway/src/Nerv.IIP.BusinessGateway.Web/Endpoints/Equipment/BusinessConsoleEquipmentEndpoints.cs)；
PDA 证据：
[`useBusinessEquipmentAlarms.ts`](../../frontend/apps/business-pda/src/composables/useBusinessEquipmentAlarms.ts)。

| 页面 / 业务对象                                                                                | 当前公开 operation 与强 ID                                                                                                                                                                       | 当前范围 / assignment                                        | 服务端筛选与分页                                                     | 权限码                                        | 动作、终态、未知态事实                                                                                                                                              | 结论 / 归属                                                                                                                                                                                                                                                         |
| ---------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ------------------------------------------------------------ | -------------------------------------------------------------------- | --------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| PDA `/equipment/alarms`；PC `/equipment/alarms`；报警列表                                      | `listBusinessConsoleEquipmentAlarms`；`alarmEventId + deviceAssetId + externalAlarmId`                                                                                                           | Organization；没有责任人/team/workcenter/workshop assignment | `deviceAssetId,deviceAssetIds,status,skip,take`；无 keyword/assignee | `business.iiot.alarms.read`                   | Raised/Acknowledged/Shelved/Cleared；无 `allowedActions/blockReasons`。PDA 只对 Raised 显示确认/搁置，PC 以本地字段判断；Cleared 应终态只读，未知状态不应出现主操作 | **当前**组织报警；**P0 缺口**范围文案/终态 [#1163](https://github.com/Mang-X/Nerv-IIP/issues/1163)、[#1160](https://github.com/Mang-X/Nerv-IIP/issues/1160)                                                                                                         |
| PDA/PC 报警处置                                                                                | `acknowledgeBusinessConsoleEquipmentAlarm`、`shelveBusinessConsoleEquipmentAlarm`、`unshelveBusinessConsoleEquipmentAlarm`；路径 `alarmEventId`，回执仍是同一 `alarmEventId`                     | Organization；actor 由请求/当前登录人带入，无责任范围裁决    | 非列表                                                               | `business.iiot.alarms.write`                  | cleared 不能确认/搁置；非 shelved 的 unshelve 是 no-op。ack first-write-wins；shelve 支持持久 `idempotencyKey`；无统一 409/422/状态回执                             | **当前**三种命令；**P0 缺口**统一回执 [#1162](https://github.com/Mang-X/Nerv-IIP/issues/1162)                                                                                                                                                                       |
| PC `/equipment/{deviceAssetId}`、telemetry history、Maintenance reliability；设备遥测/维修历史 | `getBusinessConsoleEquipmentDevice`、`getBusinessConsoleEquipmentDeviceHealth`、`queryBusinessConsoleTelemetryDeviceHistory`、`queryBusinessConsoleMaintenanceAssetReliability`；`deviceAssetId` | Organization；设备集合可按 ID 过滤，不是当前人员责任范围     | history/health/reliability 依各自 device/time window；不是任务分页   | telemetry read 或 maintenance work-order read | `dataFreshness.status=unavailable`、样本不足、空历史必须呈现“暂无数据/历史积累中”，不得从默认分数、HTTP 200 或空数组推断健康/无故障                                 | **等数据**：具体环境缺遥测或维修历史时保持真实空态；数据接入不属于页面修复。归 [#1168](https://github.com/Mang-X/Nerv-IIP/issues/1168)、[#1178](https://github.com/Mang-X/Nerv-IIP/issues/1178)、[#1179](https://github.com/Mang-X/Nerv-IIP/issues/1179) 的验收前置 |

## 4. 动作与权威回执矩阵

下表中的“冲突/失败”只记录当前可证事实。未声明 409/422 不等于业务永远不会冲突；
它表示公开契约尚未把该语义冻结，客户端不能猜。

| 域 / 动作                                           | 当前命令与前置状态                                                                                       | 幂等 / 冲突 / 失败事实                                                                                                 | 当前权威回执                                                                                                         | 缺口与 owner                                                                                                                                                                                 |
| --------------------------------------------------- | -------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| MES 开始                                            | `startBusinessConsoleMesOperationTask(operationTaskId)`；仅 Queued                                       | 必填 `idempotencyKey`；服务端持久绑定 task+action 指纹，同键并发唯一冲突自动回读；非法状态 409                         | confirmed `operationTaskId,status,changedAtUtc,idempotencyKey`                                                       | 状态动作门禁 [#1160](https://github.com/Mang-X/Nerv-IIP/issues/1160)                                                                                                                         |
| MES 暂停                                            | `pauseBusinessConsoleMesOperationTask`；仅 InProgress                                                    | 同上                                                                                                                   | 同上                                                                                                                 | 同上                                                                                                                                                                                         |
| MES 恢复                                            | `resumeBusinessConsoleMesOperationTask`；仅 Paused                                                       | 同上                                                                                                                   | 同上                                                                                                                 | 同上                                                                                                                                                                                         |
| MES 完成工序                                        | `completeBusinessConsoleMesOperationTask`；仅 InProgress                                                 | 同上；同键重放返回首次状态/时间，同键更换 action/task 拒绝；Completed/Cancelled 不可新执行                             | 同上，并可按 operation task 列表公开回读                                                                             | 同上                                                                                                                                                                                         |
| MES 报工                                            | `recordBusinessConsoleMesProductionReport`；精确 `workOrderId + operationTaskId`                         | 必填 `idempotencyKey`；数量/物料/报废原因等由领域验证并持久重放                                                        | accepted receipt 给出 GET `production-reports/{reportNo}` 回读路径；回读后才确认                                     | 错对象 P0 [#1159](https://github.com/Mang-X/Nerv-IIP/issues/1159)                                                                                                                            |
| MES 派工                                            | `assignBusinessConsoleMesDispatchTask(operationTaskId)`；关闭任务不可派，ScheduleInvalidated 不可派      | 支持 `idempotencyKey`；当前没有 Team/Workshop 代操作授权/审计协议                                                      | 通用 `accepted/downstream*`，需回读 dispatch 行                                                                      | 范围 [#1165](https://github.com/Mang-X/Nerv-IIP/issues/1165)，代操作待 [#1175](https://github.com/Mang-X/Nerv-IIP/issues/1175)、[#1179](https://github.com/Mang-X/Nerv-IIP/issues/1179) 裁决 |
| MES 工单放行                                        | `releaseBusinessConsoleMesWorkOrder(workOrderId)`；生产版本、工艺快照和设备/质量/物料门禁必须满足        | 请求 shape 有 `idempotencyKey`，但当前下游命令未以该键提供可证重放回执；OpenAPI 未声明 409/422                         | 仅 `accepted/downstream*`，必须回读工单详情                                                                          | 终态/冲突 [#1160](https://github.com/Mang-X/Nerv-IIP/issues/1160)，幂等/回执 [#1162](https://github.com/Mang-X/Nerv-IIP/issues/1162)                                                         |
| MES 工单挂起                                        | `holdBusinessConsoleMesWorkOrder(workOrderId)`；Completed/Closed/Cancelled/Scrapped 不可挂起             | 必填 reason；无幂等键或版本条件，OpenAPI 未声明 409/422                                                                | 仅 `accepted/downstream*`，必须回读 status/holdReason                                                                | [#1160](https://github.com/Mang-X/Nerv-IIP/issues/1160)、[#1162](https://github.com/Mang-X/Nerv-IIP/issues/1162)                                                                             |
| MES 工单取消                                        | `cancelBusinessConsoleMesWorkOrder(workOrderId)`；Completed/Closed/Scrapped 不可取消，已 Cancelled no-op | 必填 reason；无幂等键或标准 no-op 回执，取消还受已发料退料事实约束                                                     | 仅 `accepted/downstream*`，必须回读 status/cancelReason                                                              | [#1160](https://github.com/Mang-X/Nerv-IIP/issues/1160)、[#1162](https://github.com/Mang-X/Nerv-IIP/issues/1162)                                                                             |
| WMS 五类对象派工                                    | `assignBusinessConsoleWms{InboundOrder|PutawayTask|OutboundOrder|PickingTask|CountExecution}`；路径强 ID | Gateway 注入当前 assigner 与授权 site；必填 pool/operator、`expectedVersion + idempotencyKey`，同键同载荷可重放、异载荷拒绝 | confirmed assignment 回执包含对象、pool/operator/version/idempotencyKey；列表回读持久派工事实 | **MAN-629 已交付**；收货/上架用 receipts.manage，出库/拣货用 shipments.manage，盘点用 inventory.counts.manage |
| WMS 收货完成                                        | `completeBusinessConsoleWmsInboundOrder(inboundOrderId)`；仅 Open                                        | 必填所选工作范围、`expectedVersion + idempotencyKey`；质量门禁可能进入 PendingQualityCheck，库存失败另有状态 | `requestId + inventoryMovementId`，并回读 inbound 状态/质量门禁 | **MAN-629 已接入对象范围**；权威回执仍以精确回读为准 |
| WMS 创建上架/拣货任务                               | `createBusinessConsoleWmsPutawayTask(inboundOrderId)` / `createBusinessConsoleWmsPickingTask(outboundOrderId)`；来源强 ID 为订单 + lineNo | 上架数量不得超过已收数量且受质量门禁；拣货数量不得超过订单行且 reservation 可能失败 | 创建回执 `warehouseTaskId`，随后通过派工和任务动作推进 | 创建本身保留既有 receipts.manage / shipments.manage 权限 |
| WMS 上架/拣货开始、进度、异常、完成                 | `start/recordProgress/reportException/completeBusinessConsoleWms{Putaway|Picking}Task(warehouseTaskId)` | 必填所选范围、`expectedVersion + idempotencyKey`；任务锁与 WCS 互斥，进度累计且不可回退/超计划；上架须足量，短拣须原因 | confirmed action 回执包含 `status/version/executedQuantity/allowedActions/blockReasons`；终态动作清单为空 | **MAN-629 已交付**；PDA/PC 只消费服务端动作门禁 |
| WMS 出库复核完成                                    | `completeBusinessConsoleWmsOutboundOrder(outboundOrderId)`；Open、复核通过且有执行数量                   | 必填所选范围、`expectedVersion + idempotencyKey`；短拣/库存失败由领域状态裁决 | 回读 `inventoryPostingStatus/failureCode/failureMessage` 和 movement | **MAN-629 已接入对象范围**；不替代拣货任务完成 |
| WMS 盘点完成                                        | `completeBusinessConsoleWmsCountExecution(countExecutionId)`；仅 Open                                    | 必填所选范围、`expectedVersion + idempotencyKey`；本地/Inventory RPC 两路径均保存 movement receipt；同键同数量重放、异数量拒绝 | accepted receipt 含精确 count GET 回读路径；`requestId + inventoryMovementId` 可恢复，再核对 counted/variance/status | **MAN-629 已交付范围/派工**；写权限为 `business.inventory.counts.manage` |
| Quality 提交检验                                    | `createBusinessConsoleQualityInspectionRecordFromTask(inspectionTaskId)`；Pending→InProgress→Completed   | 必填 `idempotencyKey`；持久绑定 payload fingerprint 与 record ID，同键同载荷重放、异载荷拒绝，按 task 串行             | confirmed `inspectionRecordId,result,nonconformanceReportId/code,idempotencyKey`                                     | assignment/403/409/422 [#1167](https://github.com/Mang-X/Nerv-IIP/issues/1167)                                                                                                               |
| Quality 复检                                        | `createBusinessConsoleQualityReinspection(inspectionRecordId)`；只允许对不合格前置记录追加 successor     | 幂等/冲突由 Quality 前置链约束；公开错误未标准化                                                                       | `inspectionRecordId + attemptNumber`；result/NCR 用 `getBusinessConsoleQualityInspectionRecord` 回读                 | 统一错误/回执 [#1162](https://github.com/Mang-X/Nerv-IIP/issues/1162)                                                                                                                        |
| Quality NCR 处置/关闭                               | `submitBusinessConsoleQualityNcrDisposition(ncrId)`、`closeBusinessConsoleQualityNcr(ncrId)`             | 合法状态由 NCR 聚合裁决；当前无 `allowedActions`                                                                       | 通用 accepted，必须回读 NCR status                                                                                   | [#1160](https://github.com/Mang-X/Nerv-IIP/issues/1160)、[#1162](https://github.com/Mang-X/Nerv-IIP/issues/1162)                                                                             |
| Maintenance 报修建单                                | `createBusinessConsoleMaintenanceWorkOrder`                                                              | 必填意图级 `idempotencyKey`；超时重试同键，成功后新意图换键；同键异载荷拒绝                                            | confirmed `workOrderId,idempotencyKey,status=open`                                                                   | 报警到工单的原子复合生命周期仍归 [#1168](https://github.com/Mang-X/Nerv-IIP/issues/1168)                                                                                                     |
| Maintenance 接单/开工/暂停/待料/恢复/验证/关闭/取消 | **不存在公开命令**                                                                                       | 不得从 `assignedTechnicianUserId` 或类型名推断                                                                         | 无                                                                                                                   | **P1** [#1168](https://github.com/Mang-X/Nerv-IIP/issues/1168)                                                                                                                               |
| Maintenance 一步完成                                | `completeBusinessConsoleMaintenanceWorkOrder(workOrderId)`；仅 Open                                      | 必填意图键；同键同载荷持久重放、异载荷拒绝，按 workOrder 串行；Completed 不可新执行                                    | confirmed `workOrderId,status,changedAtUtc,idempotencyKey`                                                           | 完整生命周期仍归 [#1168](https://github.com/Mang-X/Nerv-IIP/issues/1168)                                                                                                                     |
| 报警确认                                            | `acknowledgeBusinessConsoleEquipmentAlarm(alarmEventId)`；活动报警                                       | 非幂等 first-write-wins；cleared 拒绝；超时不得盲重放                                                                  | accepted receipt 含按 `alarmEventId` 的设备报警 GET 回读路径；回读 lifecycle 后才确认                                | 领取/责任范围仍待后续                                                                                                                                                                        |
| 报警搁置/解除                                       | `shelve...`、`unshelve...`；活动报警 / Shelved                                                           | shelve 使用持久 `idempotencyKey`；unshelve 为非幂等 no-op 语义，超时先回读                                             | accepted receipt 含按 `alarmEventId` 的 GET 回读路径；回读 status/shelvedUntilUtc 后才确认                           | 领取/责任范围仍待后续                                                                                                                                                                        |
| 报警转维修                                          | 当前只能单独 `createBusinessConsoleMaintenanceWorkOrder(sourceAlarmId)`                                  | 无原子复合命令、无统一跨域 receipt                                                                                     | 新 `workOrderId`；需分别回读报警和维修工单                                                                           | 生命周期 [#1168](https://github.com/Mang-X/Nerv-IIP/issues/1168)，移动闭环 [#1178](https://github.com/Mang-X/Nerv-IIP/issues/1178)                                                           |

## 5. 前端真实呈现要求

1. 当前没有任何上述列表达到服务端 principal-bound `Self` 口径。
   `listBusinessConsoleMesDispatchTasks(assignedUserId=currentPrincipalId)` 仍只是客户端可控参数，
   必须显示“组织/业务范围待办”及查询来源；伪“我的任务”纠偏归
   [#1163](https://github.com/Mang-X/Nerv-IIP/issues/1163)，真正的当前主体查询归
   [#1164](https://github.com/Mang-X/Nerv-IIP/issues/1164)、[#1165](https://github.com/Mang-X/Nerv-IIP/issues/1165)。
2. 页面必须同时显示后端 `total`、已加载数、筛选条件和更新时间。不能以固定 `take=100/200`
   冒充全量；统一移动列表壳和分页恢复归
   [#1171](https://github.com/Mang-X/Nerv-IIP/issues/1171)。
3. 缺 org/env 或后续 required scope 时不发空 ID 请求；没有 assignment 支持时不得拉全量后
   在前端伪造 Self。
4. 列表与动作必须沿同一组强 ID：MES 报工尤其固定
   `workOrderId + operationTaskId`；路由切换、慢请求和返回不得把旧对象回写到新页面。归
   [#1159](https://github.com/Mang-X/Nerv-IIP/issues/1159)。
5. Completed、Cancelled、Closed、Cleared 和未知状态默认只读。提交前刷新/重核当前状态；
   409 后刷新详情并撤销操作区。前端隐藏不是服务端安全边界。归
   [#1160](https://github.com/Mang-X/Nerv-IIP/issues/1160)。
6. 成功结果只展示服务端回执中的业务 ID/编号；`accepted=true`、HTTP 200 或本地 toast 之后，
   若契约没有终态回执，继续回读权威对象。
7. `dataFreshness=unavailable`、空 telemetry history、空 maintenance history 显示“暂无数据/
   历史数据积累中”，不得渲染默认健康结论或补造维修记录。
8. 人员、设备、工位、库位、批次、原因码和优先级应来自可搜索目录，不要求一线用户手输
   GUID；目录归 [#1169](https://github.com/Mang-X/Nerv-IIP/issues/1169)，PDA 高频输入先由
   [#1161](https://github.com/Mang-X/Nerv-IIP/issues/1161) 收口。

## 6. 多端表面与产品裁决

当前应用目录只有 Business PDA、Business Console 和只读 `apps/screen`；仓库明确把
`apps/business-workstation` 留作 roadmap，且没有车间指挥终端 app。`apps/screen` 是挂墙大屏，
不能冒充工位机或车间机。

| 表面             | 当前事实                                                                                | 不可推断项                                                                 | 状态 / owner                                                                                                                                                                                                         |
| ---------------- | --------------------------------------------------------------------------------------- | -------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Business PDA     | 已有 MES/WMS/Quality/Maintenance/报警页面，复用 BusinessGateway business-console facade | 页面存在不等于个人范围、完整命令、终态门禁或真机闭环                       | 导航/角色聚合 **待裁决** [#1170](https://github.com/Mang-X/Nerv-IIP/issues/1170)；共用列表/动作底座 [#1171](https://github.com/Mang-X/Nerv-IIP/issues/1171)、[#1172](https://github.com/Mang-X/Nerv-IIP/issues/1172) |
| Business Console | 已有各域组织级管理列表、详情与部分命令                                                  | 默认页面不等于按角色/范围裁剪的工作台                                      | **P1 / 待裁决** [#1173](https://github.com/Mang-X/Nerv-IIP/issues/1173)                                                                                                                                              |
| 工位机           | `frontend/apps/business-workstation` 当前不存在                                         | 不得把 PDA/PC 响应式页面宣称为工位机                                       | **P1 / 待裁决** 操作工表面 [#1175](https://github.com/Mang-X/Nerv-IIP/issues/1175)；复用协议但不复用同一表面组件                                                                                                     |
| 车间机           | 当前无独立车间指挥终端 app                                                              | 不得用 `apps/screen` 替代；无 Team/Workshop 数据契约前不能做真实指挥工作台 | **P1 / 待裁决** [#1179](https://github.com/Mang-X/Nerv-IIP/issues/1179)                                                                                                                                              |

## 7. 后续 issue 归属索引

| 结论类别                               | 归属 issue                                                                                                      |
| -------------------------------------- | --------------------------------------------------------------------------------------------------------------- |
| P0 对象强 ID 一致性                    | [#1159](https://github.com/Mang-X/Nerv-IIP/issues/1159)                                                         |
| P0 终态/未知态/冲突态                  | [#1160](https://github.com/Mang-X/Nerv-IIP/issues/1160)                                                         |
| P0 PDA 高频输入                        | [#1161](https://github.com/Mang-X/Nerv-IIP/issues/1161)                                                         |
| P0 防重、幂等、统一回执和错误          | [#1162](https://github.com/Mang-X/Nerv-IIP/issues/1162)                                                         |
| P0 伪“我的任务”纠偏                    | [#1163](https://github.com/Mang-X/Nerv-IIP/issues/1163)                                                         |
| P1 主体上下文和五级范围                | [#1164](https://github.com/Mang-X/Nerv-IIP/issues/1164)                                                         |
| P1 MES assignment/range/search         | [#1165](https://github.com/Mang-X/Nerv-IIP/issues/1165)                                                         |
| 已交付 WMS assignment/range/执行命令   | [#1166](https://github.com/Mang-X/Nerv-IIP/issues/1166)（MAN-629）                                              |
| P1 Quality assignment/领取/搜索        | [#1167](https://github.com/Mang-X/Nerv-IIP/issues/1167)                                                         |
| P1 Maintenance assignment/完整生命周期 | [#1168](https://github.com/Mang-X/Nerv-IIP/issues/1168)                                                         |
| P1 可搜索目录                          | [#1169](https://github.com/Mang-X/Nerv-IIP/issues/1169)                                                         |
| 多端工作台、列表、动作协议             | [#1170](https://github.com/Mang-X/Nerv-IIP/issues/1170)–[#1173](https://github.com/Mang-X/Nerv-IIP/issues/1173) |
| 角色业务闭环                           | [#1174](https://github.com/Mang-X/Nerv-IIP/issues/1174)–[#1179](https://github.com/Mang-X/Nerv-IIP/issues/1179) |
| 产品文档、自动化、真栈验收             | [#1180](https://github.com/Mang-X/Nerv-IIP/issues/1180)–[#1182](https://github.com/Mang-X/Nerv-IIP/issues/1182) |

## 8. 本矩阵不证明的事项

- 不证明任一环境已经存在维修人员、班组长、车间组长、遥测历史或维修历史数据。
- 不证明 Quality assignment/领取或 Maintenance 完整生命周期已经实现；WMS 代码契约已交付也不等于任一部署环境已通过真实账号/真栈验收。
- 不证明任何页面通过真机、真实账号或真实全栈验收；旅程矩阵归
  [#1158](https://github.com/Mang-X/Nerv-IIP/issues/1158)，最终证据归
  [#1181](https://github.com/Mang-X/Nerv-IIP/issues/1181) 和
  [#1182](https://github.com/Mang-X/Nerv-IIP/issues/1182)。
- 不把当前 OpenAPI 中未声明的 409/422 猜成稳定公共合同；后续实现必须先更新服务契约、
  BusinessGateway、OpenAPI、generated client 和稳定导出，再由页面消费。
