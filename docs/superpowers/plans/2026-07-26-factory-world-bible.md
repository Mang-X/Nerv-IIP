# 工厂世界观设定集 v1.1（演示数据宪法）

> **v1.1 勘误与增补（2026-07-26，随 L0/L1 引擎落地实测修订）**：
> 1. §2 产线总数勘误：表头「13 产线」为笔误，按行合计为 **14 产线**（机加 5 + 装配 6 + 表面包装 3），L0 seed 已按 14 实装。
> 2. **人员权威源条款**（随 PR #1129 生效）：员工业务档案（工号/姓名/部门/岗位/在岗/班组/技能）的权威是 **MasterData `Worker` 聚合**（`user-emp-001..058` / `EMP-001..058`）；IAM 只负责登录账号，其 users 表上的姓名/工号/部门三列为账号展示冗余。**班组是车间级**（绑 `WorkshopCode` 而非工作中心）：6 班组 vs 17 工作中心无法 1:1，派工候选按 工作中心→车间→班组→在册成员 收敛，工作中心未挂车间时显式空候选、不降级全厂。
> 3. §7 质量行口径修订：不合格率**两档分层**——一期投料放大过的工单档 4.65%、其余档 1.89%，加权仍 ≈2.3%，各档独立处置权重（统一常数会使报废对账永久失败）。实测 NCR 164 / 2.531%。
> 4. §7 库存行口径修订：「批次约 5000」指**批次台账行**（实测 5035）；车间领料批 `LOT-{组件}-{工单}` 留在 MES 域，不进库存台账。
> 5. 线边库回冲规则（增补）：已完工工单在完工时按用量回冲线边库；在制工单不回冲（其线边余额即在制品）。
> 6. 后续项：原料按采购批 FIFO 消耗未铺开（采购批当前只入不出，恒等式不受影响）；仓储单据/扫码暂不建模操作人。

> 目的：为领导演示环境提供一个**内部自洽、可考证**的虚拟工厂。所有种子与历史数据引擎必须以本文为唯一事实来源生成；任何页面上出现的名字、数字、时间都应能回溯到本文的设定并互相印证。
> 原则：**真实感来自一致性**——规模可以是小型工厂，但账必须圆。

## 0. 数据分层架构（基线修订）

| 层 | 内容 | 产生方式 | 号段 | 约束 |
|---|---|---|---|---|
| L0 主数据 | 组织/人员/产线/设备/产品/BOM/工艺/日历/客户供应商/编码规则/标签模板 | 种子（幂等） | 语义编码 | 全中文，与本文逐一对应 |
| L1 背景历史 | 成立至今的订单全生命周期痕迹（含领料/报工/检验/入库/发货/应收/遥测/标签打印扫码/维修） | **历史数据引擎**：种子直写 + 历史时间戳 + 确定性伪随机（固定种子可复现） | `SO-2026-\*` `WO-2026-\*` 等年份段 | 跨域加总一致；生成后跑一致性校验器 |
| L2 现场演示 | 固定案例 + 演示当场操作 | 真实公开路径（铁律不变） | `SO-DEMO-\*` `WO-DEMO-\*` | MAN-519 白名单原样适用 |
| L3 实时模拟 | 设备遥测/报警常驻流 | #1086 模拟器常驻 + 采集连接器注册 | 设备编码 | 与 L1 历史遥测衔接（回填至今） |

> **MAN-519 基线修订条款（随本文件生效）**：原"种子不得创建结果事实"约束收窄为**仅适用于 L2 号段**；L1 背景历史块允许种子引擎直写结果事实，但必须：①历史时间戳 ②独立号段 ③附带一致性校验器 ④在演示讲稿中如实定位为"系统上线以来的历史数据"。

## 1. 工厂档案

- **名称**：宁沪减振科技有限公司（简称"宁沪减振"）
- **主营**：乘用车悬架减振器（前滑柱总成、后减振器总成）及核心零部件
- **沿革**：2019 年建厂；**2026-01-05 本平台上线**（电子历史自此开始，约 7 个月纵深——这解释了为什么系统里最早的单据是 1 月）
- **组织**（6 部门）：生产部、计划部、质量部、设备部、仓储物流部、经营部（销售+采购）
- **人员**：约 58 人在册（见 §5，员工名单由引擎按姓名池生成，工号 `EMP-0xx`）
- **工作制**：早班 08:00–16:00 / 中班 16:00–24:00（装配车间双班，机加车间早班+部分中班）；标准工作日历，周日停产保养

## 2. 车间与产线（3 车间 / 14 产线）

| 车间 | 产线 | 用途 |
|---|---|---|
| 一车间 · 机加车间 | 活塞杆线 1/2、缸筒线 1/2、精磨线 | 棒料→活塞杆；管料→缸筒 |
| 二车间 · 装配车间 | 前减装配 1/2/3、后减装配 1/2、阀系预装线 | 部件→总成 |
| 三车间 · 表面与包装车间 | 电泳涂装线、性能检测线、包装线 | 涂装/终检/包装入库 |

## 3. 设备台账（46 台，含采集点）

| 类别 | 数量 | 编码段 | 示例（型号） | 采集 |
|---|---|---|---|---|
| CNC 车床/加工中心 | 10 | `DEV-CNC-01..10` | 立式加工中心 VMC-850、数控车床 CK6150 | 主轴温度/振动/转速 |
| 数控磨床 | 4 | `DEV-GRD-01..04` | 数控外圆磨床 MK1332 | 振动/砂轮转速 |
| 装配工作站 | 12 | `DEV-ASM-01..12` | 减振器装配台（气动压装） | 压装力/节拍计数 |
| 焊接机器人 | 3 | `DEV-WLD-01..03` | 六轴焊接机器人 | 电流/温度 |
| 电泳涂装设备 | 3 | `DEV-CTG-01..03` | 电泳槽/固化炉 | 槽液温度/PH |
| 减振器性能试验台 | 4 | `DEV-TST-01..04` | 电液伺服试验台 | 阻尼力曲线采样 |
| 包装线体 | 2 | `DEV-PKG-01..02` | 自动装箱线 | 节拍计数 |
| 空压机/辅助 | 8 | `DEV-AUX-01..08` | 螺杆空压机、冷干机 | 压力/温度 |
| （既有固定案例设备保留） | — | `DEV-CNC-DEMO` | 立式加工中心 VMC-850 | L2 层专用 |

采集连接器：`CONN-OPCUA-01`（机加车间 OPC UA 网关）、`CONN-MODBUS-01`（辅助设备 Modbus）、`CONN-MQTT-01`（装配/检测 MQTT 网关）——3 个连接器覆盖全部采集设备，采集健康页应显示 3 条在线连接。

## 4. 产品与物料（约 84 SKU）

- **成品（24）**：前滑柱总成/后减振器总成 × 6 车型平台（A 级轿车 P1/P2、SUV S1/S2、MPV M1、新能源 E1）× 左右件——编码 `FG-QJ/HJ-...`，名称如「P1 平台前滑柱总成（左）」
- **半成品（18）**：活塞杆(6 规格)、缸筒(6)、阀系组件(6)——`SF-...`
- **原材料（30）**：45# 钢棒料(4)、精密钢管(4)、弹簧(6)、油封(4)、减振油(2)、连接环/防尘罩/紧固件(10)——`RM-...`
- **包材辅料（12）**：纸箱、护角、标签纸、托盘等——`PK-...`
- **BOM/工艺**：每个成品 1 条 EBOM + 1 条 MBOM（8–12 行）+ 1 条工艺路线（6–8 工序：下料→CNC 精车→精磨→阀系预装→总装→电泳→性能终检→包装）；每成品 1–2 个生产版本（V1 全部、热销 8 款有 V2 换弹簧供应商的版本演进故事）
- 既有 `SKU-DEMO-001`（汽车减振器总成）保留为 L2 固定案例，归入 P1 平台别名

## 5. 人员与角色（58 人）

| 部门 | 人数 | 角色示例 |
|---|---|---|
| 生产部 | 28 | 车间主任 3、班组长 6、操作工 19（分布三车间） |
| 计划部 | 4 | 计划主管 1、计划员 3 |
| 质量部 | 9 | 质量主管 1、检验员 6、质量工程师 2 |
| 设备部 | 6 | 设备主管 1、维修技师 4、点检员 1 |
| 仓储物流部 | 7 | 仓储主管 1、库管 4、叉车/配送 2 |
| 经营部 | 4 | 销售 2、采购 2 |

技能目录（10 项）：CNC 操作、磨床操作、压装装配、焊接、电泳工艺、性能试验、质量检验、设备维修、叉车驾驶、计划排产——操作工每人 1–3 项带等级/取证日期。

## 6. 客户与供应商

- **客户（8）**：华东汽车零部件采购中心（既有）、长三角整车一厂/二厂、华中商用车配套、比德新能源、路航售后连锁、皖江 Tier1、出口贸易（华远国际）
- **供应商（10）**：特钢棒料 2、精密钢管 1、弹簧 2、密封件 2、减振油 1、包材 2

## 7. 历史数据形状（2026-01-05 → 至今，约 29 周）

| 域 | 规模 | 形状 |
|---|---|---|
| 销售订单 | **约 3200 单**（周均 105±30，月末冲量、2 月春节低谷） | 状态分布：已收款结案 78% / 已发货待收款 8% / 在制 9% / 已下达待开工 3% / 草稿废弃 2% |
| 工单 | 约 3600 张（含内部补产） | 每单 6–8 工序任务，齐套检查记录、领料单（部分分批领）、报工记录（数量+工时，2–5 条/工单）齐全 |
| 质量 | 检验任务约 7000、不合格率 2.3%（NCR 约 160，处置：返工 60%/让步 25%/报废 15%），SPC 抽样数据连续 | 与报工数量对账一致 |
| 库存/仓储 | 期初+每日移动流水、批次约 5000（原料按采购批、成品按工单批）、收货/上架/拣货/出库任务与单据对应 | 现存量=期初+入-出 可对账 |
| 采购 | 约 480 张采购订单（原料补货节奏与生产量匹配） | 收货→质检→上架链路 |
| 财务 | 应收/应付/凭证与发货/收货一致；月末结账痕迹 | 加总一致性校验 |
| 设备 | 遥测历史（分钟级聚合回填 29 周+近 7 天原始级）、报警约 400 起、维修工单 120 张（完工含停机原因）、点检/保养记录按计划频次 | OEE/MTBF/MTTR 可算且与报警/维修对账 |
| 条码标签 | 标签模板 4 套（物料/批次/工位/成品箱贴），打印批次约 900，扫码记录与领料/入库/出库动作对应 | 当前完全没有→从零建 |

**一致性校验器**（引擎附带，生成后必跑）：订单-工单-报工-入库数量链对账；库存余额=流水累计；应收=发货未收款单据和；每台设备的报警数/停机时长与维修工单及 OEE 输入吻合；抽样 20 单全链人工可追。

## 8. 实施切分

- **P0（冻结前必须）**：L0 主数据全量（§1–§6）+ L1 历史引擎首版（§7 各域按比例可缩放，默认全量 3200 单，生成耗时实测并支持缩放参数）+ L3 常驻模拟 + 一致性校验器 + MAN-519 修订
- **P1（数据可后补，用户已确认订单数据后续可增补）**：SPC 连续曲线深度、采购-供应商绩效、多月财务结账细节
- 既有在制批次（千单规模块）收敛为本引擎的排产演示子集（未排在制单的一部分）

## 9. 号段速查

`SO-2026-#####`（历史订单）/ `WO-2026-#####` / `PO-2026-####`（采购）/ `NCR-2026-####` / `MWO-2026-####`（维修）/ `AR-2026-#####`；固定案例保留 `*-DEMO-*` 段；规模排产在制单用 `SO-SCALE-*`（并入本引擎后改 `SO-2026-*` 在制态）。

二期补登记（车间事件 / 排程 / 工程）：

| 号段 | 用途 | 归属服务 |
|---|---|---|
| `DT-2026-####` | 工作中心停机事件（设备与停机 / 产能与异常） | Mes |
| `HO-2026-#####` | 班次交接单 | Mes |
| `DEF-2026-#####` | 车间不良记录 | Mes |
| `NCR-2026-D####` | **车间不良所引用的 NCR** —— 与 Quality 侧自行开单的 `NCR-2026-####` **分段隔离** | Mes（引用） |
| `SP-2026-####` | 排程方案 | Scheduling |
| `ECO-2026-####` | 工程变更单 | ProductEngineering |
| `DOC-2026-####` | 工程文档 | ProductEngineering |

二期补登记（仓储自动化 / 循环盘点 / 来料退货）：

| 号段 | 用途 | 归属服务 |
|---|---|---|
| `CNT-2026-####` | **循环盘点单**——仓储 `count_executions.count_no` 与库存 `stock_count_tasks.count_task_code` 共用同一个号，这就是两域盘点对账的唯一锚点 | Wms + Inventory（共享规格） |
| `APPR-CNT-2026-####` | 盘点差异的审批链代理号（跨库拿不到审批服务的 GUID） | Inventory |
| `WCS-{仓储任务号}` | 外部 WCS 任务号，如 `WCS-WT-IB-PR-2026-0001-01`——派生自仓储作业任务号，无需全局序号即可幂等 | Wms |
| `RTS-{入库单}-{行号}-{检验记录}` | 供应商退货申请号（聚合自动生成，非独立序号） | Wms |
| `IR-{收货单}-RTS` | 退货检验凭据代理号，与放行凭据 `IR-{收货单}` 显式分段 | Wms |

`CNT-2026-####` 由两域共享的确定性纯函数 `WorldHistoryCountSpec` 统一发号，两侧各存一份逐字相同的
副本并各有黄金向量测试。**账面量两侧口径不同、差异量两侧口径相同**：仓储是执行侧，账面量是下发盘点单时
的快照；库存是账侧，差异要对着真实的 `StockLedger.OnHandQuantity` 算。历史盘点一律不落到 `confirmed`
（确认差异会过账调整流水、打破库存现存量恒等式），也一律不冻结台账（残留冻结会挡住演示当场的库存操作）。

二期补登记（MES 追溯断点 / 生产准备底座 / 规则排程）——**本批不新增号段，全部复用既有段**，
在此登记「谁复用了谁」，避免后来者以为这几张表没有编号约定：

| 表 | 复用的号段 | 约定 |
|---|---|---|
| `mes.output_lot_genealogies` | `LOT-{工单号}` | 一张工单一行（`produced_lot_no` 在 scope 内唯一），断点挂在**末次报工**上，数量 = 该工单报工好品累计 |
| `mes.production_report_material_consumptions` | `LOT-{组件}-{工单号}` + `MIR-{工单号}-{序号}` | 消耗批号与领料单确认的线边收料批号**逐字相同**，消耗挂在**首次报工**上；分批领料的工单取首批序号 |
| `mes.device_asset_work_center_mappings` | `DEV-*` × `WC-*`（均为 L0 既有编码） | 38 条，只映射生产设备；`DEV-AUX-01..08` 刻意不映射（服务全厂，挂产线会让其遥测被记成产量） |
| `mes.mes_sku_availabilities` | L0 既有 SKU 编码 | **黑名单表**，只记停用事实；清单只能挑非成品、非用料、非二供弹簧的原材料 |
| `mes.schedule_results` | `SP-2026-W##`（周计划号） | 每周一次基线排程 + 周内 0–3 次重排；`schedule_version` 自 1 起连续，与运行时「已有条数 + 1」衔接 |

四期补登记（设备维修备件 / 设备状态投影）：

| 号段 | 用途 | 归属服务 |
|---|---|---|
| `MRO-{类}-##` | **维修备件目录**（轴承 / 油封 / 密封圈 / 同步带 / 滤芯 / 接触器 / 气缸 / 砂轮 / 刀具 / 液压油 / 传感器 / 电极…），挂在 `maintenance.maintenance_work_order_spare_part_lines.sku_code` 上 | Maintenance |

`MRO-` 与 L0 §4 的 `RM-/PK-` 显式分段：`RM-/PK-` 是**减振器的用料**（进 BOM、进领料、进成品成本），
`MRO-` 是**维修物耗**（与产品 BOM 无关）。把「机床主轴轴承」记成 `RM-SEL-01 油封` 会让物料主数据、
BOM 与库存三处同时失真。因此 `MRO-` 备件**只存在于 Maintenance 侧的确定性 Spec**，
**不写入 MasterData SKU 主数据、不进库存台账、不产生库存流水**——历史备件消耗若真去扣库存，
会打破库存域「现存量 = 世界观流水代数和」的恒等式（WMS 那批已踩过），扣的还是不存在的账。
`maintenance_work_order_spare_part_lines` 的表注释本就写明 “not inventory balances”。
备件行本身没有独立单号（主键为 GUID，前端按 `SP-{id 后 8 位}` 呈现）。

四期补登记（设备控制指令 / 库存预留）：

| 号段 | 用途 | 归属服务 |
|---|---|---|
| `OPS-WH-*` | **设备控制指令台账**（`industrial_telemetry.device_control_commands.operation_task_id`）。两个家族：报警处置停机指令直接派生自报警号（`OPS-WH-{设备}-{点位}:{周}{槽}`），工艺参数下发按设备 × 周（`OPS-WH-SET-{设备}-{周}`） | IndustrialTelemetry |

`device_control_commands` **不是执行记录，是 Ops operation task 的投影**——真正的执行权威在 Ops，
台账存的是「下发那一刻，谁对哪台设备的哪个点位下了什么值、为什么下」的业务快照。因此历史回填
**只写台账、绝不调用 Ops 客户端**，物理上不可能产生一次下发。

**历史控制指令一律是终态（completed / failed / rejected）且带 `finished_at_utc`**，这是安全条款
不是风格：`queued` / `approval-pending` / `dispatched` 在读面上意味着「这条命令还在路上」——
详情页会持续轮询 Ops、审批面会把它列成待办，演示时任何一次「通过」都可能变成对真实设备
（或 L3 模拟连接器）的一次真实写点。终态配额每 20 条为 成功 17 / 失败 2 / 驳回 1，
按任务号的 FNV 折叠分档（不用全局序号，避免截止日推进导致位置漂移）。
「按下按钮真的动一次设备」留给演示当场走真实路径（L2）。
停机指令写的是控制通道的运行命令位 `run-command`，不是采集清单里的只读过程量；
参数指令只落在**可写设定点**（主轴转速 / 砂轮转速 / 焊接电流 / 压装力 / 阻尼力 / 槽液温度 /
槽液 PH / 气源压力）上，且设定值恒不越过报警阈值——历史参数下发不该自己制造一起报警。

`inventory.stock_reservations` **本批不新增号段**，两个家族都复用既有单号：

| 家族 | 复用的号段 | 幂等键 | 状态 |
|---|---|---|---|
| 发货拣货预留 | `OB-DO-2026-#####`（WMS 发货出库单号） | `{出库单}:pick-reserve` | `released`（发货过账时释放） |
| 齐套待领预留 | `WO-2026-#####`（已下达待开工工单） | `{工单}:kit-reserve:{物料}` | `open`（尚未领料，仍占着期初批） |

**预留只动 `reserved_quantity` 与 `ledger_version`**，绝不改 `on_hand_quantity`、
绝不写新的库存流水——库存一致性校验器按「现存量 = 世界观流水代数和（只认 `seed:world-history`）」
重算，动一下现存量或多写一笔流水当场失衡（WMS 那批已踩过，见提交 `e4deae3`）。三条都做成了
校验器条款（`ValidateReservationsAsync`）。已释放的历史预留**不回放到台账**：净效应为零，
且成品批此刻已归零，回放 `Reserve` 会被「预留超过可用量」拒绝——那是对**今天**的库存状态做的
检查，用它复核**当时**成立的占用毫无意义。未释放预留的失效时刻取 **截止日 + 180 天**：
`ExpiredStockReservationService` 会自动释放已过期的 open 预留，回填成历史时刻会让
「库存可用量」页的「已占用」列在演示途中悄悄归零。齐套预留只在库存真的够时才落
（不齐套就预留不上，这是齐套检查的本义，不是绕开异常）。

**没有「未发货的出库单」可挂**：WMS 侧的世界观出库单一律被推到 `Completed`，
且所有走到完工入库的工单其销售订单必然已发货，成品批发完即归零。硬造要么改 WMS 黄金向量、
要么在成品库凭空建账，都比「按已下达工单的齐套需求预留原料」更假——齐套预留是同一条业务链的
上游一跳（下达 → 齐套预留 → 拣货 → 领料出库），既真实又有物理库存可占。

`maintenance.maintenance_device_states` **不占号段也不是业务台账**：它是
`DeviceAssetChangedIntegrationEvent` 的最新投影（只有一个 `disabled` 布尔位），唯一用途是
给保养计划生成做闸门。「设备在跑还是在修」的视图在 IndustrialTelemetry 的
`device_state_snapshots`；维修态由工单 `Status=Open` + `AssetUnavailable` 表达。
种子按 §3 的 46 台设备镜像 MasterData 启用态（全部 `disabled=false`，变更时刻 = 上线日）。

`NCR-2026-D####` 单独分段的理由：MES 的车间不良记录需要在 `DispositionType`/`NcrCode`
上引用一张 NCR，但 MES 不能跨库向 Quality 取号，两个服务各自按序号生成会直接抢占
同一号段并在演示里出现「两张不同的 NCR-2026-0007」。用 `D` 前缀把 MES 引用侧的号
与 Quality 自开的号在字面上隔开，代价只是号码多一个字母，收益是跨域对账永不撞号。
