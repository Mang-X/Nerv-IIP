# 领导演示冲刺重规划（2026-07-23 → 07-30）与演示后路线图

> 状态：规划快照（记录 2026-07-23 决策时点的计划与依据）。数据基线：2026-07-23，open issue 94 个，近 4 周合并 247 个 PR。
> 执行看板以 Linear 演示项目为准，本文只在里程碑级变化时追加「执行状态更新」节，不逐日回填。

## 执行状态更新（2026-07-24）

规划落笔后 24 小时内，T1–T7 的开发部分已全部完成，速度大幅超前原排期：

- **T1** PR #1078 已合并（Epic #1049 随之关闭；Fixes 经复审确认保留成立）。
- **T2** #1070 已由 codex 交付 PR #1080 合并关闭（提前于 07-25 线）。
- **T3** #965 冒烟走查经独立会话一次跑全：**16/16 全绿**，新增断链票 #1083（发货出库被拒但 ERP 仍生成应收/凭证，P0）与 #1084（资本化重试致过账延迟约 5 分钟，P1）。#965 保持开启至断链票处置。
- **T4** #959 骨架先行版 PR #1085 已合并关闭（5 节点真实接入 + 8 节点显式「尚未建立关联」；后续增量为逐跳 facade 桥接票，不在冻结前赶工）。
- **T5** #799 经核实其 UI 已由 PR #931 于 07-17 交付合并，冲刺零工作量，移入 M6（终关随 #954）。
- **T7** #1061 PR #1089 已合并关闭（七模式 + 优先级交互，两轮审核收敛）。
- **范围新增**（趋势预警/设备模拟讨论产出）：#1086 设备遥测模拟器（M4，07-25 线）、#1087 设备健康评分演示切片（07-27 线）进入冲刺；#1055 裁决降 optional 移 M6。ADR 0022（PR #1082）冻结排程演进边界，M6 的 #1051/#1058/#1052 按其分层与顺序执行、合并压至演示后。
- **剩余关键路径**：#1083 修复 → #1086/#1087 → #961 彩排×2（07-28/29）→ 07-30 冻结。
- 勘误：§3 Phase A 的「wire 解包同型 bug 连修 5 处」指 5 个 bug 家族，对应 7 张票（含成对重复登记）；#763 的交付顺序以 ADR 0022 §11 为准（A 组抽象先行），本文 Phase B 表述从其。

## 1. 结论摘要

- 演示关键路径：`#1078 合并 → #1070 成本率闭环 → #965 冒烟对照表 → #959 履约时间线 → #961 终验收`。
- #961 已写明 **2026-07-30 冻结最终 commit SHA**，今日距冻结 7 天，路径上仍有三级串联；本计划把串联压成两级：#1070 立即派发、#965 非成本跳提前走查、#959 骨架先行。
- 分派原则（沿用既定多模型工作流）：Fable 负责规划/关键审核/验收彩排；codex（gpt-5.6-sol，effort medium，经 codex:codex-rescue）承接标准模式后端轻改；Claude Opus 4.8 承接前端实现；GPT 5.6 承接后端日常与互审。

## 2. 冲刺任务分派表

| # | 任务 | 执行者 | 审核 | 窗口 | 依赖 |
|---|---|---|---|---|---|
| T1 | PR #1078 审核收敛并合并；合并前确认 `Fixes #1049` 改为 `Refs #1049`（子项 #1051/#1052 未完，防 GitHub 缓存 closes 误关 Epic，同 #794/#796 前例） | 作者 + Fable 审核 | Fable | D0（7/23） | — |
| T2 | #1070 工作中心成本率公开配置（两跳：command + endpoint + facade + codegen + facade-matrix `exposed` 登记），Postgres+Redis 真实链路验证 dead letter 消除、可 replay | **codex sol medium** | **Fable（主链关键审）** | D0–D2 | — |
| T3 | #965 主链冒烟走查——先走不依赖 #1070 的跳（MRP 来源、WMS 出库、发货→应收→凭证等），产出部分对照表（每跳 × 关联键 × 自动/手工 × 是否断链） | Fable（`fullstack run` harness） | — | D0–D3 | — |
| T4 | #959 履约时间线**骨架先行**：Timeline 组件 + Gateway 页面级聚合 + 「尚未建立关联」显式态 + 单源失败隔离；节点随 T3 结论增量接入 | **Opus 4.8** | GPT 5.6 互审 + Fable 终审 | D0–D4 | 最终节点范围依赖 T3/T6 |
| T5 | #799 完工入库失败重试 + 质量 hold 时间线（纯前端，后端 #697/#698 已齐） | **Opus 4.8** | GPT 5.6 互审 | D1–D3 | — |
| T6 | #1070 合并后补 #965 成本资本化跳，输出**完整**对照表，据此冻结 #959 节点范围 | Fable | — | D3 | T2 |
| T7 | #1061 四页面统一紧急度显示模式 + 优先级交互（#1053 消费面收尾；建议补 demo:must 标签） | **Opus 4.8** | 互审 | D2–D4 | — |
| T8 | #961 终验收：第一轮彩排（主链 reset 后 5/5 + 质量/设备支线各 3/3，20–25 分钟） | Fable 主导 | — | D5（7/28） | T1–T7 |
| T9 | 第二轮彩排 + 讲稿 + 备用录屏 | Fable 主导 | — | D6（7/29） | T8 |
| T10 | 冻结 commit SHA；此后只允许修复演示阻断 | — | — | D7（7/30） | T9 |

**需 owner 裁决（D0）**：

1. **#1055 设备健康评分 V1**：标签同挂 demo:must 与 demo:optional，自相矛盾。建议**降为 optional/defer**——它是未启动项中规模最大（新评分引擎 + UI + 排产联动），且演示叙事中属加分项而非主链；若裁决为 must，需立刻单独派发并接受压线风险。
2. **#1061 是否进演示**：建议进（成本低、四页面可见度高），补 demo:must 标签。

**风险与兜底**：

- T2 若 codex 两轮审核不收敛 → Fable 直接接手（该模式在仓库已有成熟先例，风险低）。
- T4 若节点接入超时 → 未确认节点保持「尚未建立关联」显式态上场（#959 验收本身允许）。
- 彩排发现新断链 → 沿用 demo:evidence 快速循环，但**登记前先搜同型关键词查重**（此前 6 对重复登记）。

## 3. 演示后路线图（4 阶段，约 6 周）

### Phase A：架构债专项（演示后 W1–W2）——全部是"修一次消一类"的杠杆票

| 项 | 载体 | 内容 | 分派 |
|---|---|---|---|
| A1 | #877（扩范围） | CAP 消费管道统一基建：guard 包 handler + inbox 幂等 helper + 统一保存边界（或强制经 ISender 走命令 UoW）+ KnownException→死信/补偿 + 反射门禁测试 + ADR。#754/#777 为首批迁移验收，#968 为新样板 | Fable 出 ADR/设计，GPT 5.6 实现，Fable 关键审 |
| A2 | #764（+#308 并入） | 真机 Postgres 集成测试进 CI：per-PR postgres service job + nightly perf job；先定位 #920（它同时是 #754 验收测试） | codex/GPT 实现，Fable 定 CI 结构 |
| A3 | **新开票** | BusinessGateway 下游强类型 ID / ResponseData envelope 统一解包基建 + 新 facade 契约测试模板（同型 bug 已连修 5 处：#1025/#1042/#1043/#1048/#1060/#1065/#1069） | codex sol medium |
| A4 | #889 + #891 | facade 全链治理矩阵落地；#666/#677/#289/#682 登记为首批断点行 | Fable 定矩阵，前端断点派 Opus |

### Phase B：Scheduling 架构冻结 + 重排引擎（W3–W4）

- 先做一次类似 FileStorage #992 的 **Scheduling 架构冻结**（涵盖 #763 规则源拆分、#1051 执行偏差重排、#1058 局部重排引擎、#1052 拆批的边界与依赖），防止第二重域逐票野蛮生长。Fable 出冻结 ADR。
- 随后 #1051/#1058 实现（产品核心竞争力所在），GPT 5.6 后端 + Fable 关键审。

### Phase C：FileStorage MVP 切片（W5–W6 启动）

- 全量史诗约 11–15 人月，当前阶段超配。先开**「Minimal Profile MVP 总控票」**定义最小可交付边界：#992+#993+#994+#995+#996+#1011+#1012+#998（或 #999）+#1000+精简 #1006+一条消费链路（约 5–7 人月，分批执行）。
- 三个结构修正：重开或指明**扫描链承载票**（#1001 被关为 duplicate 但 #998/#1002/#1020 仍依赖它，悬空）；合并 #993/#998（同端点止血版/生产版）；#1018 与 #995 的 quota reservation 重叠收敛、#1017 并入 #1014。
- 长尾正当后置：#1016/#1017/#1019/#1015/#1009 及 #1004 的 bit-rot scrub。

### Phase D：产品 backlog 三线并行（持续，流水化）

- 大屏（#562 Epic）：**先把 #570 拆成 P0/P1/P2 子票**（单票压 16 条跨 4 域后端缺口，mini-epic 体量，不拆则长期阻塞所有真数据区块），随后 #822/#573。
- PDA #816–#818、Console C 系列 #804–#809：依赖明确、验收可判，走 Opus 前端 / GPT 后端日常互审流水。
- DemandPlanning #773–#776 作为一个 MRP 加固批次一起排（#773/#774 动同一热路径）。

### 清理项（半小时级，随手做）

- 关闭僵尸 PR #178（停滞近 2 个月、无 CI）；处置 #275（历史 Epic 大部分已实现，关闭重开残余）。
- 补标签：#754/#984/#289；裁决 #1055 标签矛盾；#1061 补 demo 标签。

## 4. 附录 A：Linear 待录入结构

> Linear 连接器接通后按此一次性录入；team 前缀 MAN。

**Project「领导演示冲刺（7/30 冻结）」**（target 2026-07-30）：

1. 合并 PR #1078 排程工作台（确认 Refs #1049）— 状态 In Review
2. #1070 成本率两跳闭环 — 派 codex sol medium，标注 Fable 关键审
3. #965 主链冒烟对照表（分两批：非成本跳先行 / 成本跳补全）
4. #959 履约时间线（骨架先行 + 节点增量接入）— Opus
5. #799 完工入库重试 + hold 时间线 UI — Opus
6. #1061 紧急度显示收尾 — Opus
7. #961 彩排×2 + 冻结 + 录屏 — Fable
8. 裁决项：#1055 must/optional、#1061 是否进演示

**Project「架构债专项」**（演示后 W1–W2）：A1 CAP 管道（#877 扩）、A2 真机 CI（#764+#308）、A3 wire 解包基建（新开 GitHub issue 后回填编号）、A4 facade 全链治理（#889/#891）。

**Project「Scheduling 架构冻结与重排引擎」**（W3–W4）：冻结 ADR、#1051、#1058、（defer：#1052、#763 并入 ADR）。

**Project「FileStorage MVP 切片」**（W5–W6 启动）：MVP 总控票、扫描链承载票重开、#993+#998 合并执行。

**Backlog（不建 project，按批次拉活）**：大屏（#570 先拆票）、PDA P2 批次、Console C 系列、MRP 加固批次 #773–#776、CAD 预览 #910–#916。

## 5. 附录 B：分派通道备注

- codex 派发经 codex:codex-rescue 子代理（gpt-5.6-sol --effort medium --write），仅限标准模式后端轻改；架构/迁移/跨服务契约不派。
- 所有主链相关 PR 由 Fable 关键审（两轮收敛基线）；前端 PR Opus 实现 + GPT 互审。
- 真机验证一律 `.\nerv.ps1 fullstack run`（Messaging=Redis + PostgreSQL profile），不使用交互 session 交付证据。
