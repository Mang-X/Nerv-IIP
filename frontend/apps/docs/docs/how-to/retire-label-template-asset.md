# 退役标签模板资产

具备资产退役权限的管理员可以在 `/barcode/templates` 中处理不再使用的当前模板文件。

1. 先将目标模板停用。
2. 在该模板行选择“资产退役”，核实模板名称。
3. 填写退役原因并选择“确认退役”。仍被引用的文件会由服务端拒绝退役。
4. 申请已受理后，使用“刷新状态”查看处理结果。

“配额已释放，文件仍保留”不代表文件已物理删除。“执行结果待核实，已暂停”和“查询窗口已过期”均不能重新执行退役。

若请求结果尚未确认，先刷新核实；同一窗口再次确认会保留原提交内容。此入口只处理当前模板文件，不提供永久历史查询或解除暂停功能。
