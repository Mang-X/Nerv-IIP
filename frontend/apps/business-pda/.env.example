# business-pda 环境变量示例
#
# VITE_NERV_IIP_API_BASE_URL — API 网关基址（BusinessGateway / PlatformGateway）
#
# Web / 本地开发：留空。此时 api-client 走相对路径 `/api/...`，
#   由 vite dev proxy（见 vite.config.ts 的 server.proxy）转发到本地网关。
#
# Capacitor / APK 构建：必须设为绝对网关基址（如 https://gateway.example.com）。
#   APK 内的 WebView 没有 vite dev proxy，相对路径 `/api/...` 会落到
#   `capacitor://` / `file://` 源上而非真实网关，请求必然失败。
#   因此打 APK 前务必设置本变量，并在构建时注入（见
#   docs/architecture/mobile-pda-deployment.md「Capacitor/APK 网关基址」一节）。
#
# 示例（仅 APK 构建需要；web/dev 请留空或注释掉）：
# VITE_NERV_IIP_API_BASE_URL=https://your-gateway-host
#
# VITE_NERV_IIP_REQUEST_TIMEOUT_MS — 全局请求超时上限（毫秒）覆写
#
# 仅供测试 / 调试使用（e2e-live 网络仿真 spec 注入短超时如 2000，避免真等 30s）。
# 【仅 DEV 生效】：只在 vite dev / vitest（import.meta.env.DEV === true）下解析；
#   生产 / APK 构建把 DEV 静态替换为 false —— 本变量整体失效，无条件回落产品默认
#   30000ms，误带进打包环境也改不了超时上限（见 src/api/request-timeout.ts 生产门禁）。
# DEV 下只接受纯正整数字符串，且钳制区间为 [100, 30000]（毫秒）：
#   空值 / 非数字 / 0 / 负数 / 小数、低于 100（瞬断级）或高于 30000（放宽上限）
#   一律回落默认，不会出现「配错值把超时改成 0、关掉或放宽」的情况。
#
# 示例（仅测试 / 调试；平时请保持注释掉）：
# VITE_NERV_IIP_REQUEST_TIMEOUT_MS=2000
