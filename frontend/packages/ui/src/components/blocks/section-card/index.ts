export {
  default as NvSectionCard,
  /** @deprecated Use `NvSectionCard` (ADR 0020 NvUI); alias removed after codemod #789. */
  default as SectionCard,
  type TrendDirection,
} from './SectionCard.vue'
export {
  default as NvSectionCards,
  /** @deprecated Use `NvSectionCards` (ADR 0020 NvUI); alias removed after codemod #789. */
  default as SectionCards,
} from './SectionCards.vue'
