/**
 * Layout — page scaffolding primitives (Nuxt UI style): centered container,
 * page-with-asides grid, responsive card grid / masonry columns, and content
 * sections. Token-driven; composes with the blocks app-shell + page-header.
 */
export {
  default as NvApp,
  /** @deprecated Use `NvApp` (ADR 0020 NvUI); alias removed after codemod #789. */
  default as App,
} from './App.vue'
export {
  default as NvAppHeader,
  /** @deprecated Use `NvAppHeader` (ADR 0020 NvUI); alias removed after codemod #789. */
  default as AppHeader,
} from './AppHeader.vue'
export {
  default as NvContainer,
  /** @deprecated Use `NvContainer` (ADR 0020 NvUI); alias removed after codemod #789. */
  default as Container,
} from './Container.vue'
export {
  default as NvPage,
  /** @deprecated Use `NvPage` (ADR 0020 NvUI); alias removed after codemod #789. */
  default as Page,
} from './Page.vue'
export {
  default as NvPageAside,
  /** @deprecated Use `NvPageAside` (ADR 0020 NvUI); alias removed after codemod #789. */
  default as PageAside,
} from './PageAside.vue'
export {
  default as NvPageGrid,
  /** @deprecated Use `NvPageGrid` (ADR 0020 NvUI); alias removed after codemod #789. */
  default as PageGrid,
} from './PageGrid.vue'
export {
  default as NvPageColumns,
  /** @deprecated Use `NvPageColumns` (ADR 0020 NvUI); alias removed after codemod #789. */
  default as PageColumns,
} from './PageColumns.vue'
export {
  default as NvPageSection,
  /** @deprecated Use `NvPageSection` (ADR 0020 NvUI); alias removed after codemod #789. */
  default as PageSection,
} from './PageSection.vue'
